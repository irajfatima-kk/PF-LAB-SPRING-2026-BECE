#include <stdio.h>
int main() {
    int guess;
    while(1) {
        printf("Guess the number: ");
        scanf("%d", &guess);
        if(guess == 7) { printf("Correct!\n"); break; }
        else if(guess > 7) printf("Too High!\n");
        else printf("Too Low!\n");
    }
    return 0;
}
